<!--pages/home-music/index.wxml-->
<!-- 搜索框 -->
<van-search disabled background="#fafafa" shape="round" placeholder="搜索您喜欢的歌曲" bind:click-input="handleSearchClick" />

<!-- 轮播图 -->
<swiper class="swiper" indicator-dots autoplay circular style="height: {{swiperHeight}}px;">
  <block wx:for="{{banners}}" wx:key="bannerId">
    <swiper-item class="swiper-item">
      <image class="swiper-image" src="{{item.imageUrl}}" mode="widthFix" bindload="handleSwiperImageLoaded"></image>
    </swiper-item>
  </block>
</swiper>

<!-- 歌曲推荐 -->
<!-- <view class="recommend-song" wx:if="{{recommendSongs.length>0}}">
  <area-header title="推荐歌曲">
  </area-header>
  <view class="song-list">
    <block wx:for="{{recommendSongs}}" wx:key="id">
      <song-item-v1 item="{{item}}"></song-item-v1>
    </block>
  </view>
</view> -->

<!-- 热门歌单 -->
<!-- <song-menu-area title="热门歌单" songMenu="{{hotSongMenu}}" wx:if="{{hotSongMenu.length>0}}"></song-menu-area> -->
<!-- 推荐歌单 -->
<!-- <song-menu-area title="推荐歌单" songMenu="{{recommendSongMenu}}" wx:if="{{hotSongMenu.length>0}}"></song-menu-area> -->

<!-- 巅峰榜 -->
<view class="ranking">
  <scroll-view class=""></scroll-view>
</view>